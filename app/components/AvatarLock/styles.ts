import styled from 'styled-components'

import { Avatar } from '@mui/material'
import { purple } from '@mui/material/colors'

export const AvatarBlock = styled(Avatar)`
  margin: 8px;
  background-color: ${purple[500]};
`
